# To compile 
javac -cp .:<libs> -d <classes-dir> *.java

e.g.
javac -cp .:libs/* -d classes/ *.java

# To run
java -cp .:<libs> Controller

.e.g.
java -cp .:../libs/* Controller

# alias
alias run="cd ~/Desktop/Databases/SLU-Twitter; javac -cp .:libs/* -d classes/ *.java; cd classes/; java -cp .:../libs/* Controller; cd .."

